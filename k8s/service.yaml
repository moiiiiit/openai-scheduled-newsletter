apiVersion: v1
kind: Service
metadata:
  name: deepseek-newsletter
spec:
  selector:
    app: deepseek-newsletter
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
