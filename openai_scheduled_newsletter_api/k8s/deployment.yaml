apiVersion: apps/v1
kind: Deployment
metadata:
  name: openai-newsletter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openai-newsletter
  template:
    metadata:
      labels:
        app: openai-newsletter
    spec:
      containers:
      - name: openai-newsletter
        image: localhost:32000/openai-newsletter:latest
        envFrom:
        - secretRef:
            name: openai-email-secrets
        - secretRef:
            name: openai-newsletter-secrets
            optional: true
        readinessProbe:
          exec:
            command:
            - python
            - -c
            - "print('ready')"
        livenessProbe:
          exec:
            command:
            - python
            - -c
            - "print('alive')"
